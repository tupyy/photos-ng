export const TIMELINE_PAGE_SIZE = 100;

// Authorization mode - when false, all permissions are granted and user fetch is skipped
export const AUTHZ_ENABLED = process.env.REACT_APP_AUTHZ_ENABLED !== 'false';
